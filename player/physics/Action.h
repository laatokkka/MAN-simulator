# pragma once

enum class Status : bool {
    MOVING_TO,                      // движение к цели
    IN_EXECUTION                    // выполнение
                                    // отмена
};

class Action {
    private:
        
    public:
};